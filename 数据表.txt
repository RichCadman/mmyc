1.注册邮箱发送表  字段：ID，邮箱号码，验证码，发送时间，
2.用户表   字段： 用户名（邮箱），姓名，密码，邀请码，添加时间
3.产品表  字段： ID，产品名称，介绍，pid
4.关联产品表，里面存放图片， 字段：ID，图片，关联ID，
5.解决方案表，  字段： ID，分类名，分类介绍，


表中要有特殊字段区分（在主页显示的图片）

后台主要的功能：
（1）上传主页图片 


新闻页面上传方法：
	新闻表里文字部分分多个字段来存储，图片也分多个字段来存，